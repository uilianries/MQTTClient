cmake_minimum_required(VERSION 2.8)

set(HEADERS_OUTPUT_DIR IoT/MQTT)

file(GLOB HEADER_FILES 
    ${CMAKE_SOURCE_DIR}/include/${HEADERS_OUTPUT_DIR}/*.h)

install(FILES ${HEADER_FILES}
    DESTINATION ${CMAKE_INSTALL_PREFIX}/include/${HEADERS_OUTPUT_DIR})
